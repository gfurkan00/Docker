events {}
http {
    server {
        listen 80;
        location / {
            resolver 127.0.0.11 valid=5s;
            set $upstream http://api:8080;
            proxy_pass $upstream;
            proxy_set_header Host $host;
        }
    }
}
